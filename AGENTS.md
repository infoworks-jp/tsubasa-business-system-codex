# AGENTS

このファイルは、このリポジトリで作業する AI コーディングエージェント向けの即時運用ガイドです。

## 目的と範囲

- 現在の実装段階は Phase 1B（商品マスター）。背景は [README.md](README.md) を参照。
- 仕様・計画・決定事項の正本は docs 配下。要件の再解釈より、まず既存文書を参照する。

## まず読む文書

- 全体像: [README.md](README.md)
- 仕様: [docs/SPECIFICATION.md](docs/SPECIFICATION.md)
- フェーズと完了条件: [docs/PHASE_PLAN.md](docs/PHASE_PLAN.md)
- テスト方針: [docs/TEST_PLAN.md](docs/TEST_PLAN.md)
- 意思決定記録: [docs/DECISIONS.md](docs/DECISIONS.md)
- 未決事項: [docs/OPEN_QUESTIONS.md](docs/OPEN_QUESTIONS.md)

## 主要コマンド

- 依存導入: pnpm install
- 開発起動: pnpm dev
- 単体テスト: pnpm test
- 型検査: pnpm typecheck
- Lint: pnpm lint
- 本番ビルド: pnpm build

## 実装境界

- App Router の UI は app と components。
- 業務ロジックは lib 配下に集約し、画面から直接データ層を増殖させない。
- 商品関連 API は app/api/products 配下、OCR 関連 API は app/api/ocr 配下。
- Supabase 連携は lib/supabase と lib/products/supabase-repository.ts を起点に扱う。

## つばさ3 本番運用ルール（最優先）

この節は実運用で発生した失敗を再発させないための累積ルール。新しい失敗・教訓が出たら、謝罪だけで終わらせず再発防止策をここへ追記する。

### 正本と反映先

- 公開Web版「つばさ3」が実際に参照する正本は Supabase の `rev2` スキーマ。日別売上の本番反映先は `rev2.daily_journal`。
- `ticket_ocr_imports` 等のOCRキューへ保存しただけでは「つばさ3へ反映済み」と扱わない。
- 公開Web版は `pages-rev2/rev2-data.js` から `rev2` の正規データを参照する。反映作業前に、対象画面がどのテーブルを参照しているか確認する。
- 本番データ登録後、必ず本番DBを再取得し、対象営業日・売上・客数等が期待値になっていることを確認する。
- DB確認が済むまで「反映完了」「完了」と報告しない。可能な範囲で公開画面側も確認してからリンクを返す。

### 営業日判定

- 味一番つばさは **毎週月曜日が定休日**。月曜日は原則として売上0円の休業日として扱う。
- 券売機ジャーナルの「集計日時」「クリア日時」をそのまま営業日とみなさない。
- 深夜0:00〜3:00は前営業日扱いという既存ルールを維持する。
- 画像を受け取ったら、日付・曜日・定休日・前回クリア日時・今回集計日時・既存の直前営業日データを突合して営業日を確定する。
- 月曜定休と矛盾する営業日候補が出た場合、曜日ルールを無視して自動登録しない。前後のジャーナルと連続性を確認する。

### 画像受領から完了報告までの固定手順

1. 画像原本から数値を読み取る。
2. 日付・曜日を確認し、月曜定休を適用する。
3. 既存 `rev2.daily_journal` の直近日付を確認し、重複・欠落・日付ずれを検査する。
4. 営業日を確定する。
5. `rev2.daily_journal` へ本番値を登録または更新する。
6. 商品別・時間帯別など画像から確実に読めるデータは、対応する `rev2` 正本へ既存形式を確認して反映する。読めない数値を推測しない。
7. 本番DBを再SELECTし、登録結果を検算する。
8. 公開Web版の参照方式・必要なら公開画面を確認する。
9. ここまで成功した場合のみ、ユーザーへ簡潔に「反映完了」と公開リンクを返す。

### 完了判定の禁止事項

- SQLが成功しただけで完了扱いしない。
- OCRテーブルへ入っただけで完了扱いしない。
- 画像を読んだだけで完了扱いしない。
- 日付を推測した状態で完了扱いしない。
- ユーザーが既に伝えた恒常ルール（月曜定休等）を毎回聞き直す前に、リポジトリ内ルールを確認する。
- 実画面または本番DBで確認できていないのに「反映されています」と断言しない。

### 失敗からの学習ログ

- 2026-08-12: OCR取込テーブル `ticket_ocr_imports` に保存しただけで「つばさ3へ反映完了」と誤報。原因は公開Web版の実参照先確認不足。以後、反映先を `rev2` 正本で確認してから作業する。
- 2026-08-12: 月曜定休を考慮せず営業日を誤判定。原因はジャーナル日時だけを見て既存営業ルールを先に照合しなかったこと。以後、曜日・定休日確認を営業日判定の最初の必須工程とする。
- 2026-08-12: 「登録操作の成功」と「ユーザーが見る公開画面への反映」を混同。以後、本番DB再取得（可能なら公開画面確認）を完了条件とする。

## Agent Approval 方針

- 承認前は、破壊的操作・外部影響のある変更を実行しない。
- 承認が必要な操作例: 依存追加/削除、DB マイグレーション変更、環境変数や認証情報の扱い変更、大規模リファクタ。
- 承認済みでも、秘密情報や本番データをリポジトリへ保存しない。
- 未承認・未検証の機能は「完成」と表現しない（根拠: [docs/DECISIONS.md](docs/DECISIONS.md)）。

## 承認前チェックリスト

- 変更目的と影響範囲を 3 行以内で明示したか。
- 既存決定事項（特にデータ改変禁止、推測補完禁止、資産分離）に反していないか。
- 追加作業が Future Features へ逸脱していないか（対象外は [docs/PHASE_PLAN.md](docs/PHASE_PLAN.md)）。

## 承認後チェックリスト

- 最小変更で実装し、無関係な整形や巻き込み変更を避ける。
- 変更後は pnpm typecheck と pnpm test を優先実行し、必要なら pnpm build まで確認する。
- 挙動変更時は docs の該当文書更新要否を確認する。

## 禁止・注意事項

- 現行資産（既存アプリ、既存 Excel、既存データ）を変更しない。
- 数値の推測補完や自動調整をしない。不一致は要確認として保持する。
- Codex 版と他系統の環境・データを共有しない。
- 以前のセッションでは OCR 拡張より環境復旧（pnpm install/dev 成功）が優先された。詰まった場合は新機能追加より開発可能状態の回復を先に行う。

## 変更提案の出し方

- 先に「何を、なぜ、どのファイルを」短く提示する。
- 実装後は、変更ファイル、検証結果、残課題の順で報告する。
- 不明点は仮定で進めず、未決事項として切り出して確認する（参照: [docs/OPEN_QUESTIONS.md](docs/OPEN_QUESTIONS.md)）。